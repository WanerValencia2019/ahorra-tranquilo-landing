---
import { Badge } from '@/components/ui/Badge.tsx';
import {
  Twitter,
  Facebook,
  Instagram,
  Linkedin,
  Github,
  Mail,
  MapPin,
  Phone,
} from 'lucide-react';

const currentYear = new Date().getFullYear();
const currentPath = Astro.url.pathname;

const footerLinks = {
  producto: [
    { name: 'Funcionalidades', href: '/funcionalidades' },
    { name: 'Precios', href: '/planes' },
    { name: 'Roadmap', href: '/roadmap' },
    { name: 'Mini Juegos', href: '/juego' },
  ],
  recursos: [
    { name: 'Blog', href: '/blog' },
    { name: 'Calculadoras', href: '/herramientas' },
    { name: 'Guías Financieras', href: '/blog' },
    { name: 'Testimonios', href: '/planes#testimonios' },
  ],
  empresa: [
    { name: 'Quiénes Somos', href: '/quienes-somos' },
    { name: 'Contacto', href: '/contacto' },
    { name: 'Seguridad', href: '/politicaspolitica-de-privacidad#seguridad' },
    { name: 'Trabaja con Nosotros', href: '/contacto' },
  ],
  legal: [
    { name: 'Privacidad', href: '/politicaspolitica-de-privacidad' },
    { name: 'Términos y Condiciones', href: '/politicas/terminos-y-condiciones' },
    { name: 'Política de Cookies', href: '/politicas/cookies' },
  ],
};

const socialLinks = [
  {
    name: 'LinkedIn',
    href: 'https://www.linkedin.com/in/waner-andres-valencia-moreno-9367a0149',
    icon: Linkedin,
  },
    {
    name: 'GitHub',
    href: 'https://github.com/WanerValencia2019',
    icon: Github,
  },
];
---

<footer class="bg-card border-t">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-10 md:py-12">
    <!-- Main Footer Content -->
    <div class="grid grid-cols-2 lg:grid-cols-6 gap-4 sm:gap-6 md:gap-8 mb-8 md:mb-12">
      <!-- Brand Column -->
      <div class="col-span-2 lg:col-span-2">
        <div class="flex items-center gap-2 mb-3 sm:mb-4">
          <a href="/" class="flex items-center gap-2 font-bold text-lg sm:text-xl text-primary">
            <div class="w-8 h-8 bg-primary rounded-lg flex items-center justify-center text-white text-sm font-bold">
              AT
            </div>
            <span>Ahorra Tranquilo</span>
          </a>
        </div>
        <p class="text-muted-foreground text-xs sm:text-sm mb-3 sm:mb-4 max-w-sm">
          Gestiona tus finanzas personales de forma inteligente. Control total de gastos, ingresos
          y presupuestos con análisis en tiempo real.
        </p>
        <Badge variant="outline" className="mb-3 sm:mb-4 text-xs">
          🇨🇴 Hecho con ❤️ en Colombia
        </Badge>
        
        <!-- Contact Info -->
        <div class="flex flex-col gap-1.5 sm:gap-2 text-xs sm:text-sm text-muted-foreground mt-3 sm:mt-4">
          <a href="mailto:soporte@support.ahorratranquilo.com" class="flex items-center gap-2 hover:text-primary transition-colors">
            <Mail className="size-3 sm:size-4 flex-shrink-0" />
            <span class="break-all">soporte@support.ahorratranquilo.com</span>
          </a>
          <div class="flex items-center gap-2">
            <MapPin className="size-3 sm:size-4 flex-shrink-0" />
            Quibdó, Chocó, Colombia
          </div>
        </div>
      </div>

      <!-- Links Columns -->
      <div>
        <h3 class="font-semibold text-foreground mb-3 sm:mb-4 text-sm sm:text-base">Producto</h3>
        <ul class="space-y-2 sm:space-y-2 sm:space-y-3">
          {
            footerLinks.producto.map((link) => {
              const isActive = currentPath === link.href || currentPath.startsWith(link.href + '/');
              return (
                <li>
                  <a
                    href={link.href}
                    class={`text-xs sm:text-sm transition-colors ${
                      isActive
                        ? 'text-primary font-semibold'
                        : 'text-muted-foreground hover:text-primary'
                    }`}
                  >
                    {link.name}
                  </a>
                </li>
              );
            })
          }
        </ul>
      </div>

      <div>
        <h3 class="font-semibold text-foreground mb-3 sm:mb-4 text-sm sm:text-base">Recursos</h3>
        <ul class="space-y-2 sm:space-y-3">
          {
            footerLinks.recursos.map((link) => {
              const isActive = currentPath === link.href || currentPath.startsWith(link.href + '/');
              return (
                <li>
                  <a
                    href={link.href}
                    class={`text-xs sm:text-sm transition-colors ${
                      isActive
                        ? 'text-primary font-semibold'
                        : 'text-muted-foreground hover:text-primary'
                    }`}
                  >
                    {link.name}
                  </a>
                </li>
              );
            })
          }
        </ul>
      </div>

      <div>
        <h3 class="font-semibold text-foreground mb-3 sm:mb-4 text-sm sm:text-base">Empresa</h3>
        <ul class="space-y-2 sm:space-y-3">
          {
            footerLinks.empresa.map((link) => {
              const isActive = currentPath === link.href || currentPath.startsWith(link.href + '/');
              return (
                <li>
                  <a
                    href={link.href}
                    class={`text-xs sm:text-sm transition-colors ${
                      isActive
                        ? 'text-primary font-semibold'
                        : 'text-muted-foreground hover:text-primary'
                    }`}
                  >
                    {link.name}
                  </a>
                </li>
              );
            })
          }
        </ul>
      </div>

      <div>
        <h3 class="font-semibold text-foreground mb-3 sm:mb-4 text-sm sm:text-base">Legal</h3>
        <ul class="space-y-2 sm:space-y-3">
          {
            footerLinks.legal.map((link) => {
              const isActive = currentPath === link.href || currentPath.startsWith(link.href + '/');
              return (
                <li>
                  <a
                    href={link.href}
                    class={`text-xs sm:text-sm transition-colors ${
                      isActive
                        ? 'text-primary font-semibold'
                        : 'text-muted-foreground hover:text-primary'
                    }`}
                  >
                    {link.name}
                  </a>
                </li>
              );
            })
          }
        </ul>
      </div>
    </div>

    <!-- Social Links -->
    <div class="border-t pt-6 sm:pt-8">
      <div class="flex flex-col md:flex-row justify-between items-center gap-3 sm:gap-4">
        <!-- Copyright -->
        <p class="text-xs sm:text-sm text-muted-foreground text-center md:text-left">
          © {currentYear} Ahorra Tranquilo. Todos los derechos reservados.
        </p>

        <!-- Social Icons -->
        <div class="flex items-center gap-4">
          {
            socialLinks.map((social) => (
              <a
                href={social.href}
                target="_blank"
                rel="noopener noreferrer"
                class="text-muted-foreground hover:text-primary transition-colors"
                aria-label={social.name}
              >
                <social.icon className="size-5" />
              </a>
            ))
          }
        </div>
      </div>

      <!-- Additional Info -->
      <div class="mt-4 sm:mt-6 text-center">
        <p class="text-[10px] sm:text-xs text-muted-foreground px-4">
          Ahorra Tranquilo es una plataforma de gestión financiera personal. No somos una entidad
          financiera ni ofrecemos servicios bancarios.
        </p>
      </div>
    </div>
  </div>
</footer>
