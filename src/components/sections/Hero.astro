---
import { Button } from '../ui/Button';
import { Badge } from '../ui/Badge';
---

<section
  id="hero"
  class="relative overflow-hidden bg-gradient-to-b from-background via-background to-accent/20 py-16 md:py-24 lg:py-32"
>
  <!-- Gradient radial animado -->
  <div class="absolute inset-0 gradient-primary-radial opacity-30"></div>

  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid gap-12 lg:grid-cols-2 lg:gap-16 items-center">
      <!-- Content -->
      <div class="space-y-8 animate-fade-in-up">
        <Badge client:load variant="default" className="gap-1">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary-foreground opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-primary-foreground"></span>
          </span>
          100% Gratis • Modo Demo Disponible
        </Badge>

        <h1 class="text-4xl font-bold tracking-tight sm:text-5xl md:text-6xl lg:text-7xl">
          Organiza tu dinero.<br />
          <span class="text-primary">Ahorra con tranquilidad.</span>
        </h1>

        <p class="text-lg text-muted-foreground sm:text-xl max-w-2xl">
          Tu vida financiera en un solo lugar, simple y sin estrés. Control total de gastos,
          ingresos y presupuestos con análisis en tiempo real.
        </p>

        <!-- Stats -->
        <div class="grid grid-cols-3 gap-4 pt-4">
          <div class="text-center space-y-1">
            <div class="text-2xl sm:text-3xl font-bold text-primary">100%</div>
            <div class="text-xs sm:text-sm text-muted-foreground">Gratis</div>
          </div>
          <div class="text-center space-y-1">
            <div class="text-2xl sm:text-3xl font-bold text-primary">🇨🇴</div>
            <div class="text-xs sm:text-sm text-muted-foreground">Hecho en Colombia</div>
          </div>
          <div class="text-center space-y-1">
            <div class="text-2xl sm:text-3xl font-bold text-primary">🔒</div>
            <div class="text-xs sm:text-sm text-muted-foreground">100% Seguro</div>
          </div>
        </div>

        <!-- CTAs -->
        <div class="flex flex-col sm:flex-row gap-4">
          <a href="https://app.ahorratranquilo.com" target="_blank" rel="noopener noreferrer">
            <Button client:load size="lg" className="w-full sm:w-auto group">
              Ir a la App
              <svg
                class="w-4 h-4 transition-transform group-hover:translate-x-1"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </Button>
          </a>
          <a href="https://app.ahorratranquilo.com/demo" target="_blank" rel="noopener noreferrer">
            <Button client:load size="lg" variant="outline" className="w-full sm:w-auto">
              Probar Demo
            </Button>
          </a>
        </div>

        <!-- Trust indicators -->
        <div class="flex flex-wrap items-center gap-4 pt-4">
          <div class="flex items-center gap-2 text-sm text-muted-foreground">
            <svg class="w-5 h-5 text-success" fill="currentColor" viewBox="0 0 20 20">
              <path
                fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                clip-rule="evenodd"></path>
            </svg>
            Sin tarjeta de crédito
          </div>
          <div class="flex items-center gap-2 text-sm text-muted-foreground">
            <svg class="w-5 h-5 text-success" fill="currentColor" viewBox="0 0 20 20">
              <path
                fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                clip-rule="evenodd"></path>
            </svg>
            Configuración en 2 minutos
          </div>
        </div>
      </div>

      <!-- Mockup Placeholder -->
      <div class="relative animate-fade-in-up animation-delay-300">
        <div class="relative rounded-2xl overflow-hidden shadow-2xl border border-border bg-card">
          <!-- 
            PLACEHOLDER: Screenshot principal del dashboard
            Tamaño recomendado: 1200x800px (ratio 3:2)
            Formato: PNG o WebP
            Optimizar para web (< 200KB)
          -->
          <img
            src="https://placehold.co/1200x800/14b8a6/ffffff?text=Dashboard+Preview+(1200x800)"
            alt="Vista previa de Ahorra Tranquilo Dashboard"
            class="w-full h-auto"
            loading="eager"
          />

          <!-- Floating cards animation -->
          <div class="absolute -top-4 -right-4 animate-float">
            <div class="bg-success text-success-foreground px-4 py-2 rounded-lg shadow-lg text-sm font-semibold">
              +$500.000 este mes 💰
            </div>
          </div>
          <div class="absolute -bottom-4 -left-4 animate-float animation-delay-200">
            <div class="bg-info text-info-foreground px-4 py-2 rounded-lg shadow-lg text-sm font-semibold">
              Meta: 80% alcanzada 🎯
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Decorative elements -->
  <div class="absolute top-1/2 left-1/4 w-72 h-72 bg-primary/10 rounded-full blur-3xl -z-10"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-info/10 rounded-full blur-3xl -z-10"></div>
</section>
