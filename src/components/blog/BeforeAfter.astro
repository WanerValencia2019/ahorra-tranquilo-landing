---
interface Props {
  before: string;
  after: string;
  metric?: string;
  improvement?: string;
}

const { before, after, metric = 'Resultado', improvement } = Astro.props;
---

<div class="my-10 grid md:grid-cols-2 gap-6">
  <!-- Antes -->
  <div class="relative group bg-gradient-to-br from-destructive/5 to-destructive/10 border-2 border-destructive/30 rounded-xl p-6 shadow-card">
    <div class="absolute top-4 right-4 text-2xl opacity-50">❌</div>
    <div class="mb-3">
      <span class="inline-block px-3 py-1 bg-destructive/20 text-destructive text-xs font-bold rounded-full uppercase tracking-wide">
        Antes
      </span>
    </div>
    <div class="text-3xl font-bold text-foreground mb-2 tabular-nums">
      {before}
    </div>
    <div class="text-sm text-muted-foreground">
      {metric} inicial
    </div>
  </div>

  <!-- Después -->
  <div class="relative group bg-gradient-to-br from-success/5 to-success/10 border-2 border-success/30 rounded-xl p-6 shadow-card">
    <div class="absolute top-4 right-4 text-2xl opacity-50">✅</div>
    <div class="mb-3">
      <span class="inline-block px-3 py-1 bg-success/20 text-success text-xs font-bold rounded-full uppercase tracking-wide">
        Después
      </span>
    </div>
    <div class="text-3xl font-bold text-foreground mb-2 tabular-nums">
      {after}
    </div>
    <div class="text-sm text-muted-foreground">
      {metric} mejorado
    </div>
    {improvement && (
      <div class="mt-3 pt-3 border-t border-success/20">
        <div class="flex items-center gap-2 text-success font-semibold">
          <span class="text-lg">📈</span>
          <span>{improvement}</span>
        </div>
      </div>
    )}
  </div>
</div>

<!-- Flecha de transformación (solo desktop) -->
<div class="hidden md:flex justify-center -mt-16 mb-10">
  <div class="bg-primary text-white px-4 py-2 rounded-full shadow-lg text-sm font-bold flex items-center gap-2">
    <span>Transformación</span>
    <span class="text-lg">→</span>
  </div>
</div>
