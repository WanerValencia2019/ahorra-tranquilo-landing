---
interface Props {
  title: string;
  icon?: string;
}

const { title, icon = '💡' } = Astro.props;
---

<div class="my-8 relative overflow-hidden rounded-xl border-2 border-primary/30 bg-gradient-to-br from-primary/5 via-primary/10 to-success/5 p-6 md:p-8 shadow-card hover:shadow-card-hover transition-all duration-300 group">
  <div class="absolute top-0 right-0 text-8xl opacity-5 group-hover:opacity-10 transition-opacity">
    {icon}
  </div>
  
  <div class="relative z-10">
    <div class="flex items-center gap-3 mb-4">
      <div class="bg-primary text-primary-foreground rounded-lg p-2 shrink-0">
        <span class="text-2xl">{icon}</span>
      </div>
      <h3 class="text-xl font-bold text-foreground">{title}</h3>
    </div>
    
    <div class="prose prose-slate dark:prose-invert max-w-none text-foreground/90">
      <slot />
    </div>
  </div>
</div>
