---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Hero from '../components/sections/Hero.astro';
import ValueProposition from '../components/sections/ValueProposition.astro';
import Features from '../components/sections/Features.astro';
import HowItWorks from '../components/sections/HowItWorks.astro';
import { Button } from '../components/ui/Button.tsx';
import { Card, CardContent, CardDescription, CardTitle } from '../components/ui/Card.tsx';
import { Badge } from '../components/ui/Badge.tsx';
import { ArrowRight, Sparkles, Calculator, Gamepad2, Rocket, BookOpen } from 'lucide-react';
---

<Layout
  title="Ahorra Tranquilo - Gestiona tus Finanzas Personales de Forma Inteligente"
  description="Control total de gastos, ingresos y presupuestos con análisis en tiempo real. La aplicación financiera más fácil de usar en Colombia."
>
  <Navbar />
  <main id="main-content">
    <!-- Hero Section -->
    <Hero />
    
    <!-- Value Proposition -->
    <ValueProposition />
    
    <!-- Features Overview -->
    <Features />
    
    <!-- How It Works -->
    <HowItWorks />

    <!-- Quick Links to Other Pages -->
    <section class="py-6 md:py-8 lg:py-12 bg-gradient-to-b from-accent/30 to-background">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-8 md:mb-12">
          <Badge variant="outline" className="mb-3 md:mb-4">
            <Sparkles className="size-3" />
            Descubre Más
          </Badge>
          <h2 class="text-2xl sm:text-3xl md:text-4xl font-semibold text-foreground mb-3 md:mb-4">
            Explora todo lo que tenemos para ti
          </h2>
          <p class="text-muted-foreground text-sm sm:text-base md:text-lg max-w-2xl mx-auto">
            Herramientas, recursos y contenido para mejorar tus finanzas
          </p>
        </div>

        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 max-w-6xl mx-auto">
          <!-- Calculadoras -->
          <a href="/herramientas" class="block group">
            <Card className="h-full hover:shadow-2xl transition-all duration-300 hover:border-primary/50">
              <CardContent className="p-4 sm:p-6 text-center">
                <div class="w-12 h-12 sm:w-16 sm:h-16 rounded-full bg-primary/10 flex items-center justify-center mx-auto mb-3 sm:mb-4 group-hover:bg-primary/20 transition-colors">
                  <Calculator className="size-6 sm:size-8 text-primary" />
                </div>
                <CardTitle className="mb-1.5 sm:mb-2 group-hover:text-primary transition-colors text-base sm:text-lg">
                  Calculadoras
                </CardTitle>
                <CardDescription className="mb-3 sm:mb-4 text-xs sm:text-sm">
                  Herramientas financieras gratis e interactivas
                </CardDescription>
                <Button client:load variant="ghost" size="sm" className="gap-2 text-xs sm:text-sm">
                  Explorar
                  <ArrowRight className="size-3 sm:size-4" />
                </Button>
              </CardContent>
            </Card>
          </a>

          <!-- Mini Juego -->
          <a href="/juego" class="block group">
            <Card className="h-full hover:shadow-2xl transition-all duration-300 hover:border-success/50">
              <CardContent className="p-4 sm:p-6 text-center">
                <div class="w-12 h-12 sm:w-16 sm:h-16 rounded-full bg-success/10 flex items-center justify-center mx-auto mb-3 sm:mb-4 group-hover:bg-success/20 transition-colors">
                  <Gamepad2 className="size-6 sm:size-8 text-success" />
                </div>
                <CardTitle className="mb-1.5 sm:mb-2 group-hover:text-success transition-colors text-base sm:text-lg">
                  Mini Juego
                </CardTitle>
                <CardDescription className="mb-3 sm:mb-4 text-xs sm:text-sm">
                  Aprende finanzas jugando y divirtiéndote
                </CardDescription>
                <Button client:load variant="ghost" size="sm" className="gap-2 text-xs sm:text-sm">
                  Jugar
                  <ArrowRight className="size-3 sm:size-4" />
                </Button>
              </CardContent>
            </Card>
          </a>

          <!-- Blog -->
          <a href="/blog" class="block group">
            <Card className="h-full hover:shadow-2xl transition-all duration-300 hover:border-info/50">
              <CardContent className="p-4 sm:p-6 text-center">
                <div class="w-12 h-12 sm:w-16 sm:h-16 rounded-full bg-info/10 flex items-center justify-center mx-auto mb-3 sm:mb-4 group-hover:bg-info/20 transition-colors">
                  <BookOpen className="size-6 sm:size-8 text-info" />
                </div>
                <CardTitle className="mb-1.5 sm:mb-2 group-hover:text-info transition-colors text-base sm:text-lg">
                  Blog
                </CardTitle>
                <CardDescription className="mb-3 sm:mb-4 text-xs sm:text-sm">
                  Guías, tips y consejos financieros
                </CardDescription>
                <Button client:load variant="ghost" size="sm" className="gap-2 text-xs sm:text-sm">
                  Leer
                  <ArrowRight className="size-3 sm:size-4" />
                </Button>
              </CardContent>
            </Card>
          </a>

          <!-- Roadmap -->
          <a href="/roadmap" class="block group">
            <Card className="h-full hover:shadow-2xl transition-all duration-300 hover:border-warning/50">
              <CardContent className="p-4 sm:p-6 text-center">
                <div class="w-12 h-12 sm:w-16 sm:h-16 rounded-full bg-warning/10 flex items-center justify-center mx-auto mb-3 sm:mb-4 group-hover:bg-warning/20 transition-colors">
                  <Rocket className="size-6 sm:size-8 text-warning" />
                </div>
                <CardTitle className="mb-1.5 sm:mb-2 group-hover:text-warning transition-colors text-base sm:text-lg">
                  Roadmap
                </CardTitle>
                <CardDescription className="mb-3 sm:mb-4 text-xs sm:text-sm">
                  Vota por las próximas funciones
                </CardDescription>
                <Button client:load variant="ghost" size="sm" className="gap-2 text-xs sm:text-sm">
                  Ver más
                  <ArrowRight className="size-3 sm:size-4" />
                </Button>
              </CardContent>
            </Card>
          </a>
        </div>
      </div>
    </section>

    <!-- CTA Final -->
    <section class="py-8 md:py-10 lg:py-12 bg-gradient-to-r from-primary to-primary/80">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-primary-foreground mb-3 sm:mb-4">
          ¿Listo para tomar control de tus finanzas?
        </h2>
        <p class="text-primary-foreground/90 text-sm sm:text-base md:text-lg mb-6 sm:mb-8 max-w-2xl mx-auto px-4">
          Únete a miles de personas que ya están mejorando su salud financiera con Ahorra Tranquilo
        </p>
        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
          <Button client:load size="lg" variant="secondary" className="gap-2">
            Ir a la App
            <ArrowRight className="size-4" />
          </Button>
          <Button client:load size="lg" variant="outline" className="gap-2 bg-primary-foreground/10 hover:bg-primary-foreground/20 text-primary-foreground border-primary-foreground/30">
            Probar Demo
            <Sparkles className="size-4" />
          </Button>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>
