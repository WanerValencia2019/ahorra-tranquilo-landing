---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Tools from '../components/Tools.tsx';
import { Calculator, ArrowLeft } from 'lucide-react';
import { Button } from '../components/ui/Button.tsx';
---

<Layout
  title="Herramientas Financieras | Ahorra Tranquilo"
  description="Calculadoras financieras gratis: meta de ahorro, presupuesto 50/30/20 y más herramientas para gestionar tu dinero."
>
  <Navbar />
  <main id="main-content">
    <!-- Hero mejorado -->
    <section class="relative py-8 md:py-12 overflow-hidden">
      <!-- Background decorativo -->
      <div class="absolute inset-0 bg-gradient-to-b from-primary/5 via-background to-success/5"></div>
      <div class="absolute top-20 left-10 w-72 h-72 bg-primary/10 rounded-full blur-3xl"></div>
      <div class="absolute bottom-20 right-10 w-96 h-96 bg-success/10 rounded-full blur-3xl"></div>

      <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <a href="/" class="inline-flex items-center gap-2 text-muted-foreground hover:text-primary transition-colors mb-8 font-medium group">
          <ArrowLeft className="size-4 group-hover:-translate-x-1 transition-transform" />
          Volver al inicio
        </a>
        
        <div class="text-center max-w-4xl mx-auto">
          <!-- Badge mejorado -->
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 border border-primary/30 mb-6 animate-fade-in">
            <Calculator className="size-4 text-primary animate-pulse" />
            <span class="text-sm font-semibold text-primary">Herramientas Gratis</span>
          </div>

          <!-- Título con gradiente -->
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold tracking-tight mb-6 text-foreground animate-fade-in-up">
            Herramientas Financieras Gratis
          </h1>

          <!-- Descripción mejorada -->
          <p class="text-xl md:text-2xl text-muted-foreground mb-8 leading-relaxed animate-fade-in-up animation-delay-200">
            Planifica y gestiona tus finanzas de manera inteligente con nuestras herramientas interactivas
          </p>

          <!-- Stats rápidos -->
          <div class="grid grid-cols-3 gap-4 md:gap-8 max-w-2xl mx-auto mt-8 animate-fade-in-up animation-delay-400">
            <div class="text-center">
              <div class="text-2xl md:text-3xl font-bold text-primary mb-1">100%</div>
              <div class="text-sm text-muted-foreground font-medium">Gratis</div>
            </div>
            <div class="text-center">
              <div class="text-2xl md:text-3xl font-bold text-success mb-1">2+</div>
              <div class="text-sm text-muted-foreground font-medium">Herramientas</div>
            </div>
            <div class="text-center">
              <div class="text-2xl md:text-3xl font-bold text-info mb-1">0</div>
              <div class="text-sm text-muted-foreground font-medium">Registro</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Tools Component -->
    <section class="relative">
      <div class="">
        <!-- Componente de herramientas -->
        <Tools client:load />

        <!-- Features de las herramientas -->
        <div class="grid md:grid-cols-3 gap-6 max-w-4xl mx-auto mt-12">
          <div class="text-center p-6 rounded-xl bg-card border border-primary/30 hover:border-primary/50 transition-all">
            <div class="w-12 h-12 rounded-full bg-primary/20 flex items-center justify-center mx-auto mb-4">
              <svg class="size-6 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="font-bold text-foreground mb-2">Resultados Instantáneos</h3>
            <p class="text-sm text-muted-foreground">Cálculos en tiempo real mientras escribes</p>
          </div>

          <div class="text-center p-6 rounded-xl bg-card border border-success/30 hover:border-success/50 transition-all">
            <div class="w-12 h-12 rounded-full bg-success/20 flex items-center justify-center mx-auto mb-4">
              <svg class="size-6 text-success" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
              </svg>
            </div>
            <h3 class="font-bold text-foreground mb-2">100% Privado</h3>
            <p class="text-sm text-muted-foreground">Tus datos nunca salen de tu navegador</p>
          </div>

          <div class="text-center p-6 rounded-xl bg-card border border-info/30 hover:border-info/50 transition-all">
            <div class="w-12 h-12 rounded-full bg-info/20 flex items-center justify-center mx-auto mb-4">
              <svg class="size-6 text-info" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            </div>
            <h3 class="font-bold text-foreground mb-2">Mobile Friendly</h3>
            <p class="text-sm text-muted-foreground">Funciona perfecto en cualquier dispositivo</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA mejorado -->
    <section class="py-8 md:py-8 relative overflow-hidden bg-gradient-to-br from-primary via-success to-primary">
      <div class="absolute inset-0 opacity-10">
        <div class="absolute top-0 left-1/4 w-96 h-96 bg-background rounded-full filter blur-3xl"></div>
        <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-background rounded-full filter blur-3xl"></div>
      </div>

      <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10 text-center">
        <div class="max-w-3xl mx-auto">
          <h2 class="text-3xl md:text-4xl font-bold text-primary-foreground mb-4">
            ¿Quieres llevar el control completo de tus finanzas?
          </h2>
          <p class="text-primary-foreground/90 text-lg md:text-xl mb-8 leading-relaxed">
            En la app encontrarás análisis automáticos, gráficas interactivas, presupuestos inteligentes y mucho más
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="https://app.ahorratranquilo.com" target="_blank" rel="noopener noreferrer">
              <Button client:load size="lg" variant="secondary" className="font-semibold text-lg px-8 py-6 h-auto shadow-xl hover:shadow-2xl hover:scale-105 transition-all">
                Ir a la App Gratis
              </Button>
            </a>
            <a href="/funcionalidades">
              <Button client:load size="lg" variant="outline" className="font-semibold text-lg px-8 py-6 h-auto bg-primary-foreground/10 text-primary-foreground border-2 border-primary-foreground hover:bg-primary-foreground hover:text-primary shadow-xl hover:shadow-2xl hover:scale-105 transition-all">
                Ver Funcionalidades
              </Button>
            </a>
          </div>

          <!-- Features adicionales -->
          <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mt-10 text-primary-foreground">
            <div class="text-center p-3 rounded-lg bg-primary-foreground/10 backdrop-blur-sm">
              <div class="text-2xl font-bold mb-1">📊</div>
              <div class="text-xs md:text-sm font-medium">Gráficas en tiempo real</div>
            </div>
            <div class="text-center p-3 rounded-lg bg-primary-foreground/10 backdrop-blur-sm">
              <div class="text-2xl font-bold mb-1">💰</div>
              <div class="text-xs md:text-sm font-medium">Presupuestos automáticos</div>
            </div>
            <div class="text-center p-3 rounded-lg bg-primary-foreground/10 backdrop-blur-sm">
              <div class="text-2xl font-bold mb-1">🎯</div>
              <div class="text-xs md:text-sm font-medium">Metas de ahorro</div>
            </div>
            <div class="text-center p-3 rounded-lg bg-primary-foreground/10 backdrop-blur-sm">
              <div class="text-2xl font-bold mb-1">📱</div>
              <div class="text-xs md:text-sm font-medium">Diseñada para cualquier dispositivo</div>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</Layout>

<style is:global>
  @keyframes blob {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(20px, -50px) scale(1.1); }
    50% { transform: translate(-20px, 20px) scale(0.9); }
    75% { transform: translate(50px, 50px) scale(1.05); }
  }

  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  .animate-fade-in {
    animation: fade-in 0.8s ease-out;
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out;
  }

  .animation-delay-200 {
    animation-delay: 0.2s;
    animation-fill-mode: backwards;
  }

  .animation-delay-400 {
    animation-delay: 0.4s;
    animation-fill-mode: backwards;
  }
</style>
